# Backend

AdonisJS 7 API for تقویم تسک.

```bash
cp .env.example .env
yarn install
node ace generate:key
node ace migration:run
node ace db:seed
yarn dev
```

Tests: `yarn test` (uses `todo_test` from `.env.test`).
