# Browser-facing Adonis API (used at build time for $env/static/public).
# Docker Compose sets this to http://localhost:<BACKEND_PORT>/api/v1 so the
# host browser talks to the published API port — not the internal service name.
PUBLIC_API_URL=http://localhost:3333/api/v1
