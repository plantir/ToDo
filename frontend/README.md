# Frontend

SvelteKit + Svelte 5 SPA for تقویم تسک.

```bash
cp .env.example .env
yarn install
yarn dev
```

The API must be running (see the root README). `PUBLIC_API_URL` defaults to `http://localhost:3333/api/v1`.
